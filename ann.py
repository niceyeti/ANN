import math
import random


class Neuron(object):
	def __init__(numInputs):
		self.Weights = [0.0 for i in range(0,numInputs)]
		self.Output
		self.
		
	"""
	Get the output of the neuron based on its current 
	"""
	def GetOutput(inputs):

